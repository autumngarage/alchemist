{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "bash -c 'GITHUB_TOKEN=$(alchemist auth-token) && export GITHUB_TOKEN && alchemist run-once --json'",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
