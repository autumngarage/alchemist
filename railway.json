{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "alchemist doctor && alchemist scan --json",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
