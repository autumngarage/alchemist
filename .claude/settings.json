{
  "_comment": "Project-owned Claude Code settings. User-specific overrides go in .claude/settings.local.json.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash scripts/branch-guard.sh"
          }
        ]
      }
    ]
  }
}
